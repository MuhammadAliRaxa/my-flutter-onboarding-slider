name: my_flutter_onboarding_slider
description: A page slider with parallex design that allows (Text) widgets or body to slide at a different speed with background.
homepage: https://github.com/MuhammadAliRaxa/my-flutter-onboarding-slider
repository: https://github.com/MuhammadAliRaxa/my-flutter-onboarding-slider
version: 1.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5+1
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
